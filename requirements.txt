# Python 3.9+ required (this repo's own floor). Editing
# LANDRY_SYSTEM_WORKBOOK_*.xlsx additionally needs LibreOffice (soffice) on
# PATH -- run `python -m landry doctor` to check both on any machine.

# Core (yfinance breaks between versions more than most — keep it current)
numpy>=1.26
pandas>=2.1
pyarrow>=14.0        # parquet cache
yfinance>=0.2.40     # data download only; tests run without it

# Landry System (landry/) — workbook I/O
openpyxl>=3.1
# anthropic>=0.40   # optional: only for `landry draft --evidence` AI drafts

# Dev
pytest>=8.0

# Web UI (webapp.py)
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27          # only needed to run the webapp tests
